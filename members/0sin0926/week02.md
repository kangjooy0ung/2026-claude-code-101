# 📝 2주차 실습 정리 — 0sin0926

## 1. 워크플로우 1바퀴 (필수)

- 작업한 것 (기능 개발 or 버그 수정): 출석체크 캘린더에 체크 표시가 뜨지 않는 버그 수정
- 과정: 탐색 → 계획(Plan Mode) → 구현 → 검증(테스트) → 커밋 → PR

### 탐색

1주차 과제에서 찾았던 오류 중, 프론트 코드에서 찾았던 ‘인덕이 옷 입히기’ 기능 오류를 재탐색함

참조값의 오류로 옷 카테고리 중 서로 다른 카테고리를 골랐음에도 같은 것을 골랐다고 인식해서 옷이 벗겨지는 오류가 있었다.

### 계획

Plan Mode로 전환해서 계획 세움

착용 판별 기준을 이미지 참조 대신 (카테고리-아이템id) 조합 키로 바꾸는 방향을 세우고, 카테고리 간 id 중복(hat의 t2 vs bottom의 t2) 충돌까지 고려하도록 명시함

### 구현

FittingRoom.js에 currentSelectionKey state를 추가하고 handleSelectItem을 **카테고리+id 키 비교로 교체**. 

### 검증

1. @babel/core+babel-preset-expo로 수정 파일 구문 검증
2. 실제 handleSelectItem 로직을 그대로 옮긴 독립 Node 스크립트로 수정 후의 토글 정상성·카테고리 간 id 비충돌까지 4가지 케이스 모두 통과 확인.

### 커밋 & PR:  fix/fitting-room-duplicate-image-toggle 브랜치 만들어서 커밋, PR

### 커밋 / PR 링크

- 커밋 https://github.com/0sin0926/walkers-high/pull/2/changes/d33ac6929b47a9948de1a9306e3daaaa15ddb4f2
- PR https://github.com/0sin0926/walkers-high/pull/2

### 느낀 점:

- 모드를 바꿔가면서 상황에 따라 적절한 모델을 선택하는 것이 편리하면서도 중요하다 느꼈다.
- 혼자 개발할 때는 검증 과정은 생략하거나 생각하지 못 했는데, 이 부분을 챙기면서 더 정확한 코드를 짤 수 있었다.

## 2. CLAUDE.md

### 로컬 루트폴더에 CLAUDE.md

- 처음 /init할 때 만든 CLAUDE.md를 기준으로 기능별로 각 폴더에 CLAUDE.md를 각각 만들게 시킴.
- 이후 코드가 수정될 때마다 코드 로직이나 아키텍쳐가 기존 요약 설명에서 유의미하게 바뀌었다면 각 폴더의 CLUADE.md를 수정하게 명령
- 바뀐 코드가 다른 기능의 폴더와 연결된 코드였다면 그 연결된 md파일도 수정

### before/after — Claude의 답이 어떻게 달라졌나:

- 역할, 로직 흐름, 엔티티 등 별로 CLAUDE.md에 적용하고, 수정 사항 역시 반영
- 따로 설정하지 않았지만 코드 내 오류도 정리해서 적어둠.

## 3. 커스텀 Slash Command + Hook

### 만든 커맨드 (`.claude/commands/...`):

### 만든 Hook (`.claude/settings.json`):

1. 실행 명령이 끝났을 때 OS에 알람 배너 띄우고, 소리 출력
2. 선택하거나 권한 설정을 해줘야할 때도 알림 Hook 설정 (1번과는 다른 소리로)
    
    ⇒ 모든 명령을 다 권한 선택 해제하지 않고, 삭제같이 되돌리기 어려운 명령어들은 권한 요청을 하게 해둔 상태여서 이에 따른 알람도 설정함
    
- 사용한 기능: Claude Code의 Stop 훅(hook) — 세션이 끝날 때마다 셸 명령을 실행하는 기능. type: "command"으로 macOS의 osascript -e 'display notification ...'을 호출해서 알림창 + 소리를 동시에 띄움.
- 적용 범위: 전역(모든 프로젝트) 선택

- 시스템 설정 > 알림에서 "스크립트 편집기(Script Editor)" 권한을 직접 켜서 알림이 실제로 뜨도록 함 (이게 없으면 명령은 성공해도 알림이 안 뜸)
- 아이콘 커스터마이징은 terminal-notifier 설치까지 시도했지만(-appIcon은 macOS 보안정책으로 무시됨, -sender는 멈춤 현상) 안 되는 걸 확인하고 포기 → 삭제, osascript 기본안으로 최종 확정

## 4. (도전) MCP / Plugin

- 연결한 MCP 서버 or 설치한 플러그인:
- 실제로 시켜본 작업 & 후기:

## 5. 회고

### 겪은 삽질:

- 처음에 고치려 시도했던 오류 : 캘린더에 날짜 추가가 안 되어서 확인해보니 입력 날짜 형식이 달랐다. 이걸 바꾸려고 yaml 파일에 한 줄 추가한 상황
    1. 처음 예상: YAML 설정 한 줄(write-dates-as-timestamps: false) 추가라 컴파일에 영향을 줄 리 없으니, ./gradlew test만 돌리면 기존 테스트가 무난히 통과할 거라 생각했다.
    2. 테스트 돌려보니 테스트 코드는 시작도 못 하고, :compileJava 단계에서 바로 실패했다.
    3. 에러 메시지와 build.gradle 내용으로 오류 내용을 찾아보니 BOM이란 걸 원격 저장소에서 못 받아온 다는 문제가 있음을 알았다. // 외부 저장소(Maven) 접근이 막혀있는 기존 환경 문제
        
        ⇒ 해결 시도 당시에는 정확히 무슨말인지 이해하지 못 했지만, 단순 로컬 환경에서는 테스트 진행이 못 한다는 결론만 내림
        
- 이후에 1번에 서술한 오류를 설계한 워크플로우에 따라 수정함

### 워크플로우가 어떻게 바뀌었나

: 수정할 코드를 바로 커밋하는 게 아니라 반드시 테스트를 거치고, 테스트 할 수 없는 환경이라면 명확한 이유를 찾는 방식으로 개발 방식이 수정되었다.

### 다음에도 또 쓸 세팅 1가지:

1. claude.md 세팅 : 공부해보지 않은 분야에 대한 개발 또한 동시에 진행해야 하기 때문에 코드 로직과 아키텍쳐에 대한 설명을 계속 만들면서 개발하면 도움이 많이 될 것 같다.
2. 알림 hook 설정 : 답변이 완료됐을 때 울리는 hook은 문제가 없지만, 권한 요청이나 선택사항에 대해서 울리는 hook은 약간의 오류가 계속 발생하고 있다. 변수가 많은 부분이기 때문에 수정을 거치면서 개발하겠다.
