# 📝 4주차 실습 정리 — InSooBeen

📅 **08.03(월) ~ 08.08(금)** — Part 3. 실전 프로젝트 · 병렬 개발 & 배포

## 프로젝트: Kinly — 가족 건강 동반자

> 📎 기획서 전체: [Notion](https://cookie-moonstone-7ce.notion.site/369a44f2e1e3807282cbc0b6c657396e)

3주차에 웹앱 전환과 그룹·초대 화면까지 끝냈고, 4주차는 핵심 3기능 중 **①비상 정보 금고를 실제로 쓸 수 있는 상태**까지 올리는 주였다. 서버 PIN·금고 토큰, 화면 전체, 그리고 이 둘이 돌아갈 **실배포 환경**이 동시에 필요했다.

그래서 이번 주의 가장 큰 변화는 기능이 아니라 **작업 방식**이다 — 세션 하나가 순차로 하는 대신 **Claude Code 세션 3개를 동시에 굴리는 병렬 3트랙 체제**로 바꿨다(배포 / 금고BE / 비상정보FE + 조율 세션 1개).

주 후반(08.07~08.08)은 그렇게 급하게 쌓은 것을 **되돌아보고 실사용 직전까지 끌어올린 구간**이다. 디자인·엔지니어링·보안 리뷰를 각각 돌려 나온 결정을 코드에 반영했고, **배포본에 로그인 수단이 하나도 없다**는 걸 뒤늦게 발견해 인증까지 붙였다.

저장소는 실제 의료·비상 정보를 다루는 프로젝트라 **개인 private 레포**(`InSooBeen/kinly`)로 운영 중입니다. 코드 열람이 필요하시면 별도로 초대해 드릴 수 있습니다.

## ✅ 체크리스트

- [x] **병렬 3트랙 체제 구축** — `git worktree` · 로컬 DB · 포트를 트랙별로 분리, 조율 이슈(#37)를 유일한 실시간 소통 매체로 규약화
- [x] **API 계약 선확정 + 스파이크 검증** — 문서만이 아니라 `ErrorStatus` 상수와 `vault.ts` 타입까지 함께 확정
- [x] **fly.io 배포 파이프라인 + 실배포** — `https://kinly-family.fly.dev` 가동, Neon(PostgreSQL) 연결, Flyway 실DB 적용 확인
- [x] **금고 BE** — PIN 설정·검증·5회 잠금, 금고 토큰(423/403 축), 접근 로그 API (서버 테스트 **84건** 통과)
- [x] **비상정보 FE** — PIN 게이트 → 정보 조회/수정 → 이력·되돌리기 → 연락처 CRUD → 접근 로그, 14개 태스크 완주 (웹 테스트 **68건** 통과)
- [x] **실서버 브라우저 검증** — 단위 테스트가 구조적으로 못 잡는 서버 결함 3건 발견 → 이슈로 분리
- [x] **전역 네비게이션 정비(#53)** — 금고에서 나갈 길을 히스토리 대신 목적지 링크로 (PR #55)
- [x] **디자인·엔지니어링·보안 리뷰 3종** — 디자인 31건 결정 / 엔지니어링 18건 발견 / 보안 감사 이슈 2건(#58·#59) 분리
- [x] **디자인 시스템 문서화 + 적용** — 외주 의뢰용 프롬프트를 `DESIGN`·`COMPONENTS`·`SCREENS` 3분할로 재구성하고, 토큰·컴포넌트를 화면 58곳에 실제 적용 (PR #60·#61)
- [x] **인증 확보** — 카카오 로그인(서버 콜백 + 서명된 state) 실계정 검증 후, 아이디/비밀번호 로그인을 추가하고 카카오는 플래그 뒤로
- [x] **비상정보 열람 로그 폐지** — 내가 부결했던 제안이 사용자 한 문장에 뒤집혔다 (계약·서버·마이그레이션 V4)
- [ ] 건강 캘린더의 공개 범위 — 방향이 하루 안에 두 번 갈려 **구현 대신 ADR 개정으로 미뤘다**

## 일별 진행 상황

### 1일차 · 2026-08-03 — 병렬 3트랙 전환

- 병렬화 준비(Phase 0)를 끝내고 세 트랙에 세션을 붙였다. 워크트리·DB·포트를 나누는 건 기계적이었고, **진짜 어려운 건 "무엇을 나누느냐"가 아니라 "무엇을 공유하느냐"**였다 — 세 세션은 서로의 미커밋 변경을 볼 수 없고 `HANDOFF.md`는 gitignore라 워크트리에 딸려오지도 않는다. 커밋과 무관하게 즉시 보이는 매체가 GitHub 이슈뿐이라, 조율 이슈 #37을 만들고 "작업 시작 / 게이트 통과 / PR 직전" 3시점에 반드시 읽는다를 규약으로 못박았다.
- **계약을 확정하기 전에 30분짜리 스파이크를 넣은 게 이 날 가장 값진 판단이었다.** 금고 토큰 검증을 서블릿 필터로 하려던 계획이 실측에서 깨졌다 — 필터에서 던진 예외가 `ExceptionAdvice`에 닿지 않아 응답 봉투가 깨지고, 그러면 프론트의 에러코드 기반 분기가 통째로 무너진다. `HandlerInterceptor`로 교체. 프론트가 화면 3~4개를 만든 뒤 발견했으면 전부 재작업이었다.
- 계약 작성이 내가 준 전제를 여러 개 뒤집었다 — 접근 로그 엔티티는 **호출처 0곳인 죽은 코드**였고, `PATCH`가 부분 수정이 아니라 **전체 교체**였으며, react-query 기본 retry가 금고 403을 2회 재시도해 **PIN 오타 한 번에 잠금 카운터가 3 소진**되고 있었다.
- 배포는 멘토 추천으로 fly.io가 올라왔는데, **ADR 0005가 이미 fly.io를 검토하고 기각한 적이 있었다.** 재검토는 정당하지만 "멘토가 추천해서"로 근거를 대체할 수는 없다고 판단해 다시 조사했고, 진짜 이득이 비용이 아니라 **`*.fly.dev`가 HTTPS를 즉시 준다는 것**(카카오 로그인·Web Push를 막고 있던 게 정확히 도메인·TLS 부재였다)임을 확인해 근거를 새로 썼다.
- **"54/54 통과"가 Gradle 캐시 값이었다.** `:test UP-TO-DATE`로 스킵된 결과를 그대로 인용하고 있었다. `--rerun-tasks`로 재확인 — 숫자는 같았지만 "통과했다"고 말한 근거 자체가 없었던 것이라 함정으로 문서에 남겼다.
- 조율 라운드에서 **내가 건 게이트가 과했고 트랙 쪽이 맞았다.** "Neon 실측 전엔 배포 PR을 머지하지 않는다"고 선언했는데, 그 PR은 설정 코드뿐이라 실측이 실패해도 바뀌는 게 없었다. **실측 결과가 뒤집을 수 없는 것을 게이트로 걸면 뒤 트랙만 막는다** — 정정하고 머지했다.

### 2일차 · 2026-08-04 — 배포 완주

- `fly deploy`가 헬스체크 타임아웃으로 계속 죽었다. 원인은 `openssl rand -base64 64`가 **64자마다 넣는 줄바꿈** — 명령 치환이 끝의 개행만 지우고 중간 개행은 남겨서 Java `Base64.getDecoder()`가 거부하고 있었다. `tr -d '\n'`으로 해결.
- 전날 Neon 실측이 **오염된 상태**로 남아 있었다. 앱 머신을 아예 끈 채로 잰 CU-hour는 트래픽이 원래 0이라 낮게 나오는 게 당연해서, "Hikari `minimum-idle: 0` 설정이 기여했다"를 증명하지 못한다. 앱을 켠 채로 30분 재측정 → **compute Idle, 누적 0.15 CU-hour**(월 환산 약 4.5, 무료 한도 100의 5% 미만)로 게이트 통과. 측정 오염은 "무엇을 쟀나"가 아니라 **"어떤 상태에서 쟀나"**의 문제였다.
- ADR에 적어둔 재검토 조건은 **아무도 능동적으로 트리거하지 않는다**는 걸 알기 때문에, 상시 가동 재전환·CU-hour 재측정을 문서에만 남기지 않고 **카카오 로그인 이슈(#28)에 직접 물려뒀다** — 실사용이 시작되는 지점에서 반드시 보이도록.

### 3일차 · 2026-08-05 — 금고BE·FE 머지, 실서버 검증

- 배포 → 금고BE(PR #45) → 비상정보FE(PR #51) 순서대로 머지. 조율 세션이 금고BE PR을 **계약 문서와 한 절씩 대조해 직접 읽었다** — 테스트 84개 통과는 "자기 코드가 자기 의도대로 돈다"의 증명이고, 확인해야 할 건 **다른 트랙이 이미 그 계약을 전제로 화면을 만들고 있다는 점**이었다. 계약 변경 0건.
- FE 트랙은 금고BE가 머지되기 전에도 멈추지 않으려고 화면을 먼저 만들었는데, **목킹 레이어를 따로 두지 않고 실제 `vault.ts` 함수를 처음부터 호출**했다(테스트에서만 `vi.mock`). 그 결과 서버가 들어온 뒤 **교체할 목킹 코드가 0줄**이었고, 실서버 검증이 "코드를 바꾸고 확인"이 아니라 "그대로 두고 확인"이 됐다.
- 실서버를 실제로 때려보고 **단위 테스트가 구조적으로 못 잡는 결함 3건**을 찾았다.
  1. **PIN 실패 카운터가 롤백된다**(#52) — 엔티티를 바꾼 뒤 같은 트랜잭션 안에서 예외를 던져 flush가 취소된다. 로직은 맞고 트랜잭션 경계만 틀렸다. 관측 지점이 커밋 경계 너머라 **요청을 두 번 보내야만** 드러난다.
  2. **본문이 깨진 모든 요청이 400이 아니라 500**(#47 계열) — "연락처 PATCH가 500이다"에서 멈췄으면 연락처만 고쳤을 것이다. 원인을 끝까지 밀어서 `ExceptionAdvice`에 핸들러가 없는 **API 전역 문제**임을 알았다.
  3. **연락처 `priority` 중복** — FE가 새 항목 순번을 `contacts.length`로 계산해 중간을 지우면 기존 값과 겹쳤다. `max(priority) + 1`로 고치고 회귀 테스트 추가.
- 헤드리스 크로미움으로 전 흐름을 돌려 계약의 위험 항목을 눈으로 확인(콘솔 에러 0건). **retry 검증은 원래 계획대로는 불가능했다** — 판정 기준이던 `remainingAttempts`가 위 1번 결함 때문에 항상 5로 고정이라, **응답 값에서 요청 횟수로 관측 지점을 옮겨** 같은 질문에 답했다.

### 4일차 · 2026-08-06 — 전역 네비게이션

- 화면이 다 붙고 나서야 드러난 문제: **금고에 들어가면 나갈 길이 브라우저 히스토리뿐**이었다(#53). 상단바 로그아웃을 각 화면이 슬롯으로 넘기는 구조였는데 실제로 넘기는 화면이 그룹 목록 하나뿐이라, 금고에서는 상단바가 빈 껍데기가 됐다. **탈출구를 각 화면의 책임으로 두면 한 곳만 빠뜨려도 사용자가 갇힌다.**
- 로고를 홈 링크로, 로그아웃을 상단바 상시 노출로 올리고, `navigate(-1)` "뒤로" 버튼은 **목적지가 정해진 링크**로 교체했다(탭을 누른 만큼 되감아야 했고 새로고침 뒤엔 갈 곳이 아예 없었다). 금고 URL이 `/persons/{personId}/...`라 그룹 정보를 갖고 있지 않은 문제는 `?from=` 쿼리로 출발지를 넘겨 해결 — **새로고침·북마크로 열려도 살아남는 폴백**을 함께 뒀다.

### 5일차 · 2026-08-07 — 리뷰만 하는 날 (디자인 · 엔지니어링 · 보안)

- 코드를 한 줄도 안 짜고 `web/` 전체를 세 번 훑었다. **디자인 리뷰 7패스(31건 결정)**, **엔지니어링 리뷰 4섹션(18건 발견, 회귀 4건)**, **보안 감사** 순. 결과물은 PR #60 — 문서만, 코드 변경 0건이다.
- **이 날 가장 큰 실수는 리뷰 대상이 아니라 리뷰 방식이었다.** `docs/design/`에 이미 확정된 설계 문서가 있는 걸 알면서도 "새로 구성해달라"는 말만 듣고 그냥 시작했다. 읽어보니 팔레트·버튼 7종·배지 5종이 이미 정해져 있었고, 내가 "발견"한 것 중 셋은 거기 이미 답이 적혀 있었으며, 더 나쁜 건 **내 결정 3개가 그 문서와 정면 충돌**했다는 것이다. **"새로 쓰라"는 지시를 받아도 기존 문서를 먼저 읽어야 한다** — 충돌을 표로 만들어 하나씩 정리하고 학습으로 기록했다.
- 리뷰 결과를 **다른 모델/세션에 다시 검증**시켰더니 **내 결정 3개를 뒤집었다.** 그중 하나는 보안 방어를 깰 뻔했다 — 접근 로그가 과하게 쌓이는 걸 보고 `refetchOnWindowFocus: false`를 걸기로 했는데, **그 재조회가 클라이언트가 서버의 423(잠금)을 받는 유일한 경로**였다. 껐으면 서버는 잠겼는데 화면엔 캐시된 의료 정보가 남는다. 게다가 반쪽짜리라 "연락처 탭에 있으면 잠기고 정보 탭에 있으면 안 잠기는" 상태가 됐을 것이다.
- 검증이 **내가 못 본 데이터 손실 경로**도 찾았다. 나는 "편집 중 잠그기를 누르면 입력이 사라진다"까지만 봤는데, 실제로는 저장 요청이 423을 받으면 자동으로 `lock()`이 불려서 **복용약을 10분 넘게 입력하다 저장을 누르는 순간 입력이 증발**했다. 원인은 게이트가 잠금 상태에서 children을 아예 렌더하지 않는 **조건부 렌더 한 줄**이었다 — 오버레이로 바꾸면 세 경로가 한 번에 해결된다.
- **내가 쓴 문서가 내가 고친 버그를 재생산했다.** 이력 화면의 diff 방향이 거꾸로인 걸 찾아 고치기로 했는데, 정작 내가 쓴 `SCREENS.md`에 그 거꾸로 된 설명이 적혀 있었다. 서버 코드를 읽어 다이어그램으로 다시 그렸다 — **"구현자는 스펙 문서를 읽는다"**가 검증이 남긴 지적이었다.
- AI 목업 생성은 계정 인증(`organization verification required`)에서 막혔다. 우회 불가라 **실제 토큰과 웹폰트를 쓴 정적 HTML 목업**(390×852 6화면)으로 대체했는데, 결과적으로 이게 낫다 — 이미지 모델은 한글을 깨뜨려 그리고, HTML은 48px·16px을 픽셀 단위로 검증할 수 있다.
- 보안 감사에서 나온 것 중 즉시 안 고칠 두 건은 이슈로 분리했다 — **런타임 컨테이너가 root로 실행**되고 dev 로그인 차단이 `fly.toml` 한 줄에만 의존하는 문제(#58), **PIN이 평문 입력란**이라 화면·브라우저 폼 히스토리에 남는 문제(#59).

### 6일차 · 2026-08-08 — 디자인 적용 · 인증 · 배포 (PR #60·#61)

- **내가 부결한 제안이 사용자 한 문장에 뒤집혔다.** 프론트가 올린 "비상정보 열람 로그를 없애자"를 근거 3개를 세워 부결했는데, *"가족용으로 정보 올리는 거 자체가 공유 목적이야"* 한 문장에 무너졌다. 부결은 "조회 로그가 억제하는 대상이 실재한다"에 기대고 있었고, 공유가 목적이면 억제할 대상이 없다. **교훈은 "부결하지 말았어야 했다"가 아니다** — 근거를 명시적으로 써뒀기 때문에 *어느 전제가 깨졌는지* 정확히 짚을 수 있었다. 번복 사실과 이유를 회신 문서 맨 위에 박아뒀다(안 써두면 몇 주 뒤에 "왜 조회 로그가 없지"로 또 뒤집힌다).
- 리뷰에서 정한 디자인 결정을 실제로 적용했다 — 토큰 전면 교체(소비처 58곳), Button 7종×3크기·Badge·Chip·Callout 신설, 텍스트 링크 8곳을 터치 영역 있는 버튼으로 교체, `aria-invalid` 연결. 근거는 **고령 사용자를 하한선으로 잡는다**(본문 16px·터치 48px·대비 4.5:1)는 한 줄이다.
- **배포본에 로그인 수단이 0개였다.** 개발용 로그인이 `@Profile("!prod")`라 운영에는 아예 없다는 걸 이제야 발견했다. 카카오 로그인을 서버 콜백 방식으로 붙이고(서명된 state로 CSRF 방어, 오픈 리다이렉트를 5가지 적대적 입력으로 테스트, 토큰을 쿼리가 아닌 프래그먼트로 전달) 실계정으로 검증했는데, **가족 시나리오(부모·자녀·형제)를 끝까지 돌려보려면 카카오 계정을 여러 개 만들어야 했다.** 그래서 아이디/비밀번호 로그인을 추가하고 카카오는 지우지 않고 플래그 뒤로 내렸다 — 검증까지 끝난 코드를 지우면 다시 짜고 다시 검증해야 한다.
- **비상정보 화면만 500이 나는데 새 계정으로는 재현이 안 됐다.** 갈린 지점은 "빈 금고에는 복호화할 게 없다" — 암호화 키가 다르면 AES-GCM은 쓰레기값이 아니라 예외를 던지므로 **값이 든 계정만** 터진다. PIN·로그인은 다른 알고리즘이라 멀쩡해서 더 헷갈렸다. 진단은 **값을 노출하지 않고** `.env`와 실행 중인 프로세스 환경의 키를 각각 해시 앞 16자만 찍어 대조했다.
- **같은 실수를 두 번 했다.** 서버 테스트 66건이 무더기로 깨졌는데, 실패 목록이 내가 건드리지도 않은 곳이라 마이그레이션을 의심했다. 실제로는 환경변수를 안 실어 앱 컨텍스트가 안 뜬 것이었다. 첫 번째 때 함정 문서에 넣었으면 두 번째가 없었다 — 그래서 이 날 `PITFALLS.md`를 신설해 `HANDOFF`("지금 상태")와 역할을 갈랐다.
- 배포(v7) 후 운영에서 확인한 것: 콜드스타트 **34.8초**, 대소문자 다른 아이디의 중복 가입 차단(`lower(login_id)` 유니크 인덱스가 실제로 도는 걸 실증), **계정 열거 차단**(없는 아이디와 비밀번호 오류가 같은 코드·같은 문구), 개발용 로그인이 운영에서 404. `fly deploy`가 뱉은 "machine not found"는 **실패가 아니라 CLI의 조회 경쟁**이었다 — 머신은 떠서 헬스체크까지 통과했다. **릴리스 상태와 실제 가동 상태는 다를 수 있다.**
- **방향이 하루 안에 두 번 갈렸다.** 오전엔 "공유가 목적이니 조회 로그를 없애라", 오후엔 "공개 범위를 적용해 프라이버시를 확보하자"였고 **둘은 양립하지 않는다.** 그 자리에서 구현으로 들어가지 않고 **모순을 지적하고 ADR을 먼저 쓰자고 제안했다** — 새 도메인의 데이터 모델·공개 범위·권한 위임을 추측 위에 지으면 부모님 반응을 본 뒤 전부 다시 만진다.

## 이번 주에 남은 것

- **계약을 코드로 확정하고, 확정 전에 스파이크로 실측한다.** 두 트랙이 동시에 짜고 있으면 잘못된 전제 하나가 양쪽 재작업이 된다.
- **게이트는 미리 값의 형태로 못박되, 어느 산출물의 게이트인지를 틀리면 안 된다.**
- **통과했다는 숫자보다 그 숫자가 실제 실행 결과인지**를 먼저 본다(캐시된 테스트 결과, 오염된 측정).
- 실서버 검증은 단위 테스트의 중복이 아니라 **관측 지점이 다른 검증**이다 — 이번 주 서버 결함 3건이 전부 거기서 나왔다.
- **AI의 리뷰 결과를 다시 검증시키는 값이 크다.** 뒤집힌 결정 3개 중 하나는 보안 방어를 깰 뻔했고, 하나는 내가 못 본 데이터 손실 경로였다. 다만 그 전에 **기존 문서를 읽는 게 먼저다** — 안 읽고 시작한 리뷰는 이미 답이 있는 것을 다시 "발견"한다.
- **판단의 근거를 명시적으로 써두면 뒤집힐 때 싸다.** 부결 근거 3개를 적어뒀기에 전제 하나가 깨진 순간 무엇이 무너졌는지 바로 짚였다.
- **양립하지 않는 지시가 오면 구현이 아니라 모순 지적이 먼저다.** 추측 위에 새 도메인을 지으면 실사용 뒤에 전부 다시 만진다.

## 다음 할 것

- 비상정보 입력 마찰 줄이기(#54, 혈액형 선택·자주 쓰는 항목 칩) — 리뷰에서 나온 진짜 버그 8건도 함께
- 보안 이슈 처리 — 컨테이너 root 실행·프로필 기본값(#58), PIN 평문 입력란(#59), 매핑 없는 경로 처리 잔여분(#47)
- 건강 캘린더 **ADR 먼저** — 공개 범위·권한 위임을 정한 뒤에 구현
- 실사용 투입(부모님) 후 재검토: 카카오 플래그 해제(#28), 상시 가동 재전환·Neon 재측정·1GB VM OOM 리스크
- 남은 핵심 기능: ②안전 체크인, ③컨디션 추세
